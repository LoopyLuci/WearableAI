#ifndef ERROR_CODES_H
#define ERROR_CODES_H
// ErrorCode enum is defined in common_types.h alongside the types that use it.
// This file exists for forward-compat if error codes need their own header.
#include "common_types.h"
#endif
